Описание dashboard SCADA Optimization-1775378768100.json почему-то в строках запроса к БД содержит неверные имена переменных, точнее, начиная с какого-то момента, они все называются \"name\" = 'PRS140t13.D13', хотя при импорте такого явно кривого дашборда все запросы в новой панели откуда-то узнают правильные имена. Я приложил словарь targets.json, чтобы правильно соотносить имена на картинке с именем в БД и файле данных.


df_min df_max
Схема та же: первый ключ – timestamp. df_min – для ограничений снизу, df_max – для ограничений сверху.
