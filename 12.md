set -a; . ./.env; set +a
docker cp seed-users.rb gitlab:/tmp/seed-users.rb
docker exec \
  -e ADMIN_PASSWORD="$ADMIN_PASSWORD" -e USER_PASSWORD="$USER_PASSWORD" \
  -e ADMIN_USERNAME="$ADMIN_USERNAME" -e USER_USERNAME="$USER_USERNAME" \
  gitlab gitlab-rails runner /tmp/seed-users.rb

docker exec gitlab gitlab-rails runner 'puts "users: "+User.pluck(:username).join(", "); puts "groups: "+Group.pluck(:path).join(", ")'
